

        <a href="/case/{{singleCase._id}}/edit">
            <button>
                Update {{singleCase.name}}
            </button>
        </a>
        
        {{name}}

        <div class="title">Manufacturer:</div>
        {{singleCase.brand}}

        <div class="title">Size:</div>
        {{singleCase.size}}

        <div class="title">Viewport Type:</div>
        {{singleCase.viewPort}}

        <div class="title">Watercooling Friendly:</div>
        {{singleCase.waterCool}}

        <form action="/case/{{singleCase._id}}?_method=DELETE" method="POST">
            <input type="submit" value="Delete {{singleCase.name}}">
        </form>
